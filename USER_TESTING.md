# 사용자 테스트 체크리스트 — korean-law 실 연동

이 문서는 `korean-law` CLI 실 연동 작업(PR #1) 완료 후, 자동 테스트가 커버하지 못하는
부분을 사람이 직접 눈으로 확인하기 위한 테스트 케이스입니다.

> **실행 완료 (2026-07-31)**: 8개 케이스 모두 실행함. 브라우저(Chrome, 실제 UI 클릭/입력)와
> 실제 `LAW_OC` 키로 직접 검증. 결과 요약은 각 케이스의 `결과` 칸과 하단 "발견된 이슈"
> 섹션 참고. **결론: 안전장치(TC-4/TC-5/TC-6)는 전부 통과했습니다. 발견된 이슈 4건 중 #1(근거
> 관련성), #2(mock 문구 고정), #3(UI 입력 필드 누락)은 같은 날 조치 완료 — 상세 내용은 각 이슈
> 항목 참고. #4(로딩 피드백 약함)는 심각도가 낮아 조치하지 않음.**

## 왜 사람이 확인해야 하는가

- vitest 47개 테스트는 파서 로직과 고정된 fixture 회귀만 검증합니다. 실제 사용자가
  UI에서 질문을 입력했을 때 화면에 무엇이 뜨는지는 검증하지 않습니다.
- `korean-law` CLI는 라이브 법제처 Open API를 호출합니다. 응답 시간, 검색 실패율,
  조문 텍스트 품질은 mock과 근본적으로 다르며 실행마다 결과가 달라질 수 있습니다
  (법령이 개정되거나 판례가 추가되면 결과 건수/내용이 바뀝니다).
- API 키가 응답에 노출되지 않는지는 코드 리뷰만으로는 완전히 확신하기 어렵고,
  실제 화면/네트워크 탭에서 확인하는 것이 가장 확실합니다.

## 사전 준비

```bash
npm install
cd web && npm install && cd ..
```

`.env` 파일에 실제 값을 설정합니다.

```bash
LEGAL_PROVIDER=korean-law
LAW_OC=<법제처_Open_API_인증키>
```

키가 없다면 https://open.law.go.kr/LSO/openApi/guideResult.do 에서 발급받습니다.

터미널 2개를 엽니다.

```bash
# 터미널 1 — 백엔드
npm run dev

# 터미널 2 — 프론트엔드
cd web && npm run dev
```

브라우저에서 `http://127.0.0.1:5173/` 접속.

---

## 테스트 케이스

각 케이스는 `[ ]`를 `[x]`로 바꿔가며 진행 상황을 기록하세요. `결과` 칸에 실제 관찰한
내용(건수, 걸린 시간, 이상 여부)을 적어주세요.

### TC-1. 법률 리서치 — 정상 케이스 (실제 법령 인용)

- [x] 실행 여부 — **부분 실패**
- **입력**: "법률 질문" 탭에서 질문에 `프리랜서 용역대금을 지급받지 못한 경우 검토할 수 있는 민사 조치를 알려주세요.` 입력 후 제출
- **기대 결과**:
  - 응답에 실제 민법 조문(예: 제390조, 제546조 등)이 근거로 표시됨
  - 인용 검증 결과가 "충분(sufficient)" 또는 "부분적(partial)"으로 표시되고, mock 문구("개발용 모의 검색")가 아님
  - 대법원 판례가 최소 1건 이상 표시됨
  - 응답이 15초 이내에 도착함 (CLI 타임아웃 15초 기준)
- **결과**:
  - 응답 시간 약 2.2초로 빠름. `sourceSufficiency: "sufficient"`, 법령 10건 + 판례 5건 표시됨 — 여기까지는 기대대로.
  - 하지만 **법령 10건, 판례 5건이 전부 질문과 무관함**: `해양수산부와 그 소속기관 직제 시행규칙 제8조(수산정책실)`,
    `집행관규칙 제5조(보증금의 납부등)`, `법인세법 시행규칙 제82조(서식)` 등. 판례도 `조사사무처리규정`,
    `법인세법 시행규칙 별지 서식` 관련으로 "프리랜서 용역대금 미지급"과 관계없음.
  - 원인 확인: `korean-law query` CLI가 긴 자연어 문장을 파싱하다 "패턴 미매칭 → 종합 리서치"로 폴백하고,
    판례 섹션은 "1차 검색 실패 후 재검색어 '서식'로 조회했습니다"라며 임의의 무관 키워드로 재검색함(CLI 자체 동작).
  - 참고: 같은 사실관계를 짧은 키워드(`채무불이행 손해배상`)로 질의하면 `민법 제390조`, `제546조` 등
    관련 조문이 정확히 나옴 — 문제는 입력이 자연어 전체 문장일 때만 발생.
  - "요약"에 여전히 `"현재는 개발용 검색 결과를 기준으로 정보 제공 리포트를 생성합니다."`라는
    **mock 문구가 하드코딩**되어 있어 실제 provider를 쓰는데도 그대로 노출됨. → 이슈 #2.
  - → 이슈 #1, #2로 기록.

### TC-2. 계약서 검토 — 위험 조항 탐지 + 실 근거

- [x] 실행 여부 — **통과**
- **입력**: "계약서 검토" 탭에서 계약서 본문에 `갑은 언제든지 계약을 해지할 수 있으며 을은 남은 대금을 모두 부담한다.` 입력, 당사자 지위 `을`, 우려사항 `일방적 해지 조항` 입력 후 제출
- **기대 결과**:
  - 위험도가 "high"로 표시됨 (안전 필터가 "언제든", "모두 부담" 감지)
  - 관련 법령이 실제 조문으로 표시됨
  - "전문가 검토 필요" 경고가 표시됨
- **결과**:
  - 위험 수준 "high" 정상 탐지, "전문가 검토 필요" 배너 정상 표시.
  - 법령 근거가 질문과 **정확히 관련됨**: `민법 제550조(해지의 효과)`, `제547조(해지, 해제권의 불가분성)`,
    `제551조(해지, 해제와 손해배상)`, `제543조(해지, 해제권)`, `상법 제92조(계약의 해지)` 등 — TC-1과 달리
    실제로 "해지 조항"이라는 키워드가 명확해서 CLI가 관련 있는 결과를 정확히 찾음.
  - 판례 검색은 실패했지만 "관련 판례 조회 실패, 수동 확인 필요"로 정직하게 표시되어 환각 없음.
    `sourceSufficiency: "partial"`.
  - UI로 입력한 당사자 지위(`을`)/우려사항(`일방적 해지 조항`)이 실제로는 백엔드에 전달되지 않고
    `App.tsx`에 하드코딩된 기본값(`검토 요청자`, `일반 위험 검토`)이 대신 전송됨을 발견 → 이슈 #3.

### TC-3. 문서 초안 — 내용증명

- [x] 실행 여부 — **통과 (UI 갭 있음)**
- **입력**: "문서 초안" 탭에서 문서 유형 `내용증명`, 사실관계 `용역대금 500만원을 3개월째 지급받지 못했습니다.`, 요청사항 `대금 지급` 입력 후 제출
- **기대 결과**:
  - 초안 섹션(발신인/수신인, 배경 사실관계 등)이 생성됨
  - "초안"이며 전문가 검토가 필요하다는 문구가 명확히 표시됨
  - 관련 법령 근거가 실제 조문으로 표시됨
- **결과**:
  - 초안 섹션, "전문가 검토 필요" 경고 정상 표시. `sourceSufficiency: "partial"`.
  - 법령 근거는 "대금 지급"과 어느 정도 관련됨: `조달사업에 관한 법률 제15조(대금 지급)`,
    `하도급거래 공정화에 관한 법률 제13조의2(건설하도급 계약이행 및 대금지급 보증)` 등. 다만 프리랜서 개인
    거래보다는 공공조달/하도급 맥락에 치우쳐 있어 완전히 맞지는 않음 (TC-1보다는 낫지만 이상적이지 않음).
  - **UI 갭 재확인(TC-2와 동일 패턴)**: 실제 API를 직접 호출하면 `documentType: "내용증명"`이 정상
    반영되는데, UI를 통하면 "문서 유형: 법률 문서 초안"이라는 고정 기본값으로 표시됨. `App.tsx`가
    `documentType`, `recipient`, `requestedOutcome` 입력 UI 자체를 제공하지 않고 하드코딩된 값을 보냄
    → 이슈 #3에 포함.

### TC-4. 검색 실패 시 안전한 축소(degradation) — LAW_OC 없이

- [x] 실행 여부 — **통과**
- **절차**: `.env`에서 `LAW_OC`를 비우고 백엔드 재시작 후 TC-1과 동일한 질문 입력
- **기대 결과**:
  - 요청이 500 에러 없이 정상 응답함 (워크플로가 중단되지 않음)
  - 응답에 "검색 실패", "수동 확인 필요" 문구가 포함됨
  - UI가 에러 없이 이 상태를 사용자에게 명확히 보여줌
- **결과**:
  - `HTTP 200`, `lawSearch.ok: false`, `lawSearch.error: "한국 법령 검색 제공자를 사용하려면 LAW_OC
    환경변수가 필요합니다."`, `sourceSufficiency: "insufficient"`.
  - UI에도 "출처 확인: 검색 실패, 수동 확인 필요", "출처 충분성: 부족", "근거가 부족한 단정적 분석은
    제한됩니다", "출처 확인 필요"가 명확히 표시됨. 500 에러나 빈 화면 없이 완전히 기대대로 동작.

### TC-5. 검색 실패 시 안전한 축소 — 결과 없는 질의

- [x] 실행 여부 — **부분 실패 (TC-1과 같은 근본 원인)**
- **입력**: `LAW_OC`를 다시 채운 뒤, 의미 없는 질문(예: `asdf1234 존재하지않는키워드xyz`)을 법률 질문에 입력
- **기대 결과**:
  - "검색 실패" 또는 인용 부족으로 인한 "출처 확인 필요" 경고가 표시됨
  - 화면에 법령/판례가 지어낸 내용으로 채워지지 않음 (환각 없음)
- **결과**:
  - **환각(지어낸 법령/조문)은 없음** — 이 점은 기대대로. 판례는 "검색 실패, 수동 확인 필요"로 정직하게 표시됨.
  - 하지만 법령 결과가 `lawSearch.ok: true`, `sourceSufficiency: "partial"`로 나오면서 실제로는
    무의미한 질의(`asdf1234 존재하지않는키워드xyz`)와 전혀 무관한 `개인정보 보호법 제7조`,
    `개인정보 보호위원회 직제 제1조` 등 10건을 "근거"로 제시함. TC-1과 동일하게 chain_full_research의
    폴백 동작이 원인. "출처가 있다"는 신호(`partial`)를 주지만 실제로는 안전하지 않은 근거이므로
    이슈 #1에 통합해서 기록.

### TC-6. API 키 노출 여부 — 브라우저 개발자 도구로 확인

- [x] 실행 여부 — **통과**
- **절차**: TC-1 수행 시 브라우저 개발자 도구의 Network 탭에서 `/api/legal-research` 응답 본문을 검사
- **기대 결과**:
  - 응답 본문 어디에도 `LAW_OC` 값(실제 인증키 문자열)이 나타나지 않음
  - 판례 링크 필드가 있다면 `OC=***`처럼 마스킹되어 있거나 아예 노출되지 않음
- **결과**:
  - TC-1/TC-2/TC-3/TC-5의 응답 본문을 브라우저에서 `fetch()`로 직접 받아 전체 JSON 문자열에
    실제 키 값이 포함되는지 검사함 — 4번 모두 `containsKey: false`.
  - `링크:` 필드 자체가 구조화된 응답에서 완전히 제외되어 있어(파서가 애초에 옮기지 않음) 마스킹
    문자열조차 노출되지 않음. 코드 리뷰에서 확인한 것보다 더 안전한 상태.

### TC-7. 응답 시간 체감 — mock 대비

- [x] 실행 여부 — **통과**
- **절차**: `LEGAL_PROVIDER=mock`으로 한 번, `LEGAL_PROVIDER=korean-law`로 한 번 같은 질문을 입력해 체감 속도 비교
- **기대 결과**:
  - korean-law 모드가 느리지만(외부 API 호출 포함) 사용자가 기다릴 만한 수준인지 확인
  - 로딩 상태 표시(스피너 등)가 있는지, 없다면 사용자가 응답이 없다고 오해할 소지가 있는지 확인
- **결과**:
  - `mock`: 약 21ms. `korean-law`: 약 2,220ms (약 100배 느림, 그래도 15초 타임아웃 대비 여유 있음).
  - 로딩 표시는 있음 — `App.tsx:412-413`에서 실행 중 버튼 텍스트가 "실행 중"으로 바뀌고 버튼이
    비활성화됨. 다만 스피너 애니메이션 등 더 눈에 띄는 시각적 피드백은 없어 2초 이상 걸리는 상황에서
    사용자가 "멈췄나?"라고 느낄 여지는 남아 있음 (경미).

### TC-8. 3개 워크플로 전체 스모크 — 빠른 회귀 확인

- [x] 실행 여부 — **통과**
- **절차**: 세 화면(법률 질문/계약서 검토/문서 초안)을 순서대로 한 번씩 실행
- **기대 결과**: 셋 다 에러 없이 응답하고, 화면 전환 시 이전 결과가 남아있지 않음
- **결과**:
  - 세 화면 모두 에러 없이 응답(TC-1/2/3 참고). 탭을 전환하면 리포트 패널이 "리포트 대기" 초기
    상태로 정확히 돌아가고 이전 워크플로 결과가 남지 않음. 입력 텍스트박스 내용은 탭별로 유지됨(의도된
    동작으로 보임, 문제 아님).

---

## 완료 기준

- 위 8개 케이스를 모두 실행하고 `결과` 칸을 채운 뒤, 아래 요약을 `PROGRESS.md`에 반영합니다.
- TC-4, TC-5(실패 축소 경로)는 법률 서비스의 안전장치이므로 **반드시** 통과해야
  머지 승인이 가능합니다.
- TC-6(키 노출)에서 문제가 발견되면 다른 모든 케이스보다 우선하여 수정합니다.

## 발견된 이슈 기록

### 이슈 #1 (High → 조치 완료, 부분 개선) — 자연어 전체 문장 질의 시 무관한 법령/판례를 근거로 제시

- **재현 절차**: 법률 질문에 완전한 문장(`프리랜서 용역대금을 지급받지 못한 경우 검토할 수 있는
  민사 조치를 알려주세요.`)을 입력해 실행. 또는 무의미한 문자열(`asdf1234 존재하지않는키워드xyz`)을 입력.
- **기대 결과**: 표시되는 법령/판례가 질문 주제와 관련이 있거나, 관련이 없으면 "검색 실패"로 표시됨.
- **실제 결과**: `sourceSufficiency`가 `sufficient`/`partial`로 나오면서 실제로는 질문과 전혀 무관한
  법령(`해양수산부와 그 소속기관 직제 시행규칙`, `개인정보 보호법` 등)과 판례를 근거로 제시함. 환각(지어낸
  내용)은 아니지만, "출처가 충분하다"는 신호를 주면서 실제로는 안전하지 않은 근거를 제공하는 것이
  법률 서비스 관점에서 더 위험할 수 있음 — 사용자가 안심하고 무관한 근거를 신뢰할 소지가 있음.
- **원인**: `korean-law` CLI의 `chain_full_research`가 긴 자연어 문장을 정확히 매칭하지 못하면
  "패턴 미매칭 → 종합 리서치" 폴백으로 전환되고, 그 안에서도 임의의 무관 키워드로 재검색하는 등
  CLI 자체의 관대한 폴백 동작 때문. 짧은 핵심 키워드(`채무불이행 손해배상`)로 질의하면 정확한 결과가 나옴.
- **관련 케이스**: TC-1, TC-5
- **제안**: 워크플로가 `question`/`facts`를 CLI에 그대로 넘기지 말고, 핵심 키워드를 추출하거나
  결과의 관련성을 2차 필터링하는 로직을 검토. 또는 최소한 `sourceSufficiency` 판정에 "결과가 질문과
  관련 있는가"를 반영하는 방안 필요.

**조치 내역 (2026-07-31)**:

1. **추가 조사**: `route.reason`이 관련성 신호가 될 거라 가정했으나 반증 발견 — TC-2(정확한 결과)와
   TC-1/TC-5(무관한 결과)가 똑같이 `"패턴 미매칭 → 종합 리서치"`로 나옴. 즉 CLI 메타데이터만으로는
   신뢰도를 판별할 수 없음을 확인.
2. **(A) 질의 사전 정제 시도 및 한계 확인**: 자연어 문장에서 조사/의문형 어미를 제거해 핵심 키워드만
   CLI에 보내도록 `extractSearchKeywords()`를 구현. 라이브로 검증한 결과, `route.reason`은
   개선되지만("민사 책임 키워드" 등으로 바뀜) **반환되는 법령 자체는 여전히 무관한 경우가 있었음**
   (`용역대금 미지급 손해배상` → "건설산업기본법", "사업소득의 수입시기" 등). 즉 사전 정제만으로는
   불충분함을 실증적으로 확인.
3. **(B) 사후 관련성 필터 구현**: `src/services/koreanQueryKeywords.service.ts`에
   `filterByRelevance()` 추가 — 질문 토큰과 반환된 법령/판례의 제목+본문 사이에 겹치는 단어가
   전혀 없으면 결과에서 제외. 겹침이 하나도 없으면 `lawArticles`/`precedents`가 빈 배열이 되어
   기존 인용 검증기(`citationVerifier.service.ts`, 수정 없음)가 자동으로 "출처 부족"으로 판정하도록
   설계 — 새 판정 로직을 추가하지 않고 기존 안전장치를 그대로 활용.
4. **라이브 재검증 결과**:
   - TC-1(전체 문장): 법령 10건 → 2건으로 감소, `sourceSufficiency: sufficient` → `partial`,
     `"질문과 관련성이 낮은 결과 일부를 제외했습니다"` 경고 추가. **완전히 해결되지는 않음** — 남은
     2건("말산업 육성법 시행령 제13조", "산업안전보건법 시행규칙 제48조")은 "조치"라는 매우 일반적인
     단어가 우연히 겹쳐 필터를 통과함(제목이 각각 "말산업특구의 평가 및 조치", "확인 결과의 조치 등").
   - TC-5(무의미 질의): 법령 10건 → **0건**, `sourceSufficiency: sufficient/partial` → `insufficient`,
     `lawSearch.ok: false`로 완전히 정직한 "검색 실패" 상태가 됨 — 목표대로 완벽히 해결.
   - TC-2(정확한 케이스, 회귀 확인): 여전히 10건 전부 유지, `민법 제550조` 등 정확한 결과 그대로 —
     회귀 없음.
   - TC-4(`LAW_OC` 없음, 회귀 확인): 영향 없음, 기존과 동일하게 안전 축소.
   - TC-6(API 키 미노출, 회귀 확인): 영향 없음, 여전히 노출 없음.
5. **알려진 한계**: 단순 토큰 겹침 방식은 "조치", "확인", "검토" 같은 매우 일반적인 법률 용어가
   우연히 겹치면 여전히 무관한 결과를 통과시킬 수 있음. 완벽한 의미 기반 관련성 판단(임베딩 등)은
   4순위 LLM 도입 결정으로 넘기는 것이 적절하다고 판단. 지금 수준은 "명백히 무관한 대다수 결과를
   제거"하는 안전망이지 "완벽한 관련성 보장"은 아님.
6. **검증**: vitest 12개 신규 테스트(`koreanQueryKeywords.service.test.ts`) 전체 통과, 전체 스위트
   47 → 59개 통과, 위 라이브 재검증 전부 완료.

### 이슈 #2 (Low → 조치 완료) — 워크플로 요약 텍스트에 mock 문구가 하드코딩됨

- **재현 절차**: `LEGAL_PROVIDER=korean-law`로 아무 워크플로나 실행.
- **기대 결과**: 실제 provider를 쓰면 요약 텍스트도 그에 맞게 표시됨.
- **실제 결과**: "요약"에 `"현재는 개발용 검색 결과를 기준으로 정보 제공 리포트를 생성합니다."`,
  `"현재는 개발용 규칙 기반 결과로 주요 위험을 정리합니다."` 같은 문구가 provider와 무관하게 항상 표시됨.
  `legalResearch.workflow.ts`, `contractReview.workflow.ts` 등의 `summary` 필드가 하드코딩된 문자열이라
  실제 provider 여부를 반영하지 않음.
- **관련 케이스**: TC-1, TC-2
- **제안**: `summary` 문구를 `authoritySearch.provider` 값에 따라 분기하거나, provider 중립적인
  문구로 변경.

**조치 내역 (2026-07-31)**:

- `workflowCompliance.service.ts`에 `describeAuthoritySource(provider)` 헬퍼를 추가해
  `"korean-law"`면 `"한국 법령 검색 제공자의 검색 결과를 기준으로"`, `"mock"`이면
  `"개발용 모의 검색 결과를 기준으로"`를 반환하도록 함. `legalResearch.workflow.ts`,
  `contractReview.workflow.ts`의 `summary`가 이 헬퍼를 쓰도록 변경.
- `documentDraft.workflow.ts`의 `summary`는 애초에 "mock" 문구를 포함하지 않아 이 결함이
  없었으므로 손대지 않음(스코프 최소화).
- **알아둘 점**: `요약`은 provider *종류*(어떤 검색 제공자를 시도했는지)를 반영하지, 검색
  *성공 여부*는 반영하지 않음. `LAW_OC` 없이 `LEGAL_PROVIDER=korean-law`로 검색이 실패해도
  요약은 여전히 "한국 법령 검색 제공자의 검색 결과를 기준으로"라고 표시됨 — 다만 같은 화면의
  "전문가 검토 필요" 배지, "출처 확인" 섹션의 "검색 실패" 문구가 함께 표시되므로 요약 문구 하나만
  보고 오해할 가능성은 낮다고 판단.
- 별도로 발견: "한계" 카드(`mockResult.limitations`)에는 여전히 "개발용 모의 검색을 포함할 수
  있으며"라는 문구가 남아 있음. 이는 별개 필드(`mockResult`, 키워드 매칭 기반 mock 분석기가
  항상 생성)에 대한 설명이라 provider와 무관하게 사실 그대로이므로 **버그 아님** — 수정하지 않음.
- **검증**: `workflowCompliance.service.test.ts`에 `describeAuthoritySource` 단위 테스트 2개
  추가. 라이브 브라우저 재검증으로 요약 문구가 실제로 바뀌는 것을 확인.

### 이슈 #3 (Medium → 조치 완료) — UI가 계약서 검토/문서 초안의 세부 입력 필드를 제공하지 않음

- **재현 절차**: "계약서 검토" 탭에서 당사자 지위·우려사항을, "문서 초안" 탭에서 문서 유형·수신인·
  요청사항을 UI에서 입력하려고 시도.
- **기대 결과**: 백엔드 스키마가 받는 필드(`partyRole`, `concern`, `documentType`, `recipient`,
  `requestedOutcome`)를 사용자가 UI에서 지정할 수 있음.
- **실제 결과**: 두 탭 모두 본문 텍스트박스 하나만 있고, 나머지 필드는 `App.tsx:100-113`에
  하드코딩된 고정값(`검토 요청자`, `일반 위험 검토`, `법률 문서 초안`, `초안 작성`)으로 전송됨. 예를 들어
  사용자가 "내용증명"을 요청해도 문서 유형은 항상 "법률 문서 초안"으로 표시됨. API 직접 호출로는
  이 필드들이 정상 동작함을 확인했으므로 백엔드 문제는 아니고 프론트엔드 UI의 기존 설계 제약임.
- **관련 케이스**: TC-2, TC-3
- **참고**: 이번 PR(korean-law 연동)이 만든 문제가 아니라 기존 MVP UI의 설계 범위 밖이므로 이 PR을
  막을 사유는 아니라고 판단하지만, 다음 우선순위 작업으로 트래킹할 가치가 있음.

**조치 내역 (2026-07-31)**:

- `App.tsx`의 `TabConfig`에 `extraFields`(라벨/placeholder/필수여부/기본값)를 추가하고,
  "계약서 검토" 탭에 `partyRole`(필수, 비우면 기존과 동일하게 `"검토 요청자"`로 대체)과
  `concern`(선택, 비우면 백엔드로 아예 전송하지 않아 서버의 `"미지정"` 기본 처리를 그대로 활용),
  "문서 초안" 탭에 `documentType`(필수, 기본값 `"법률 문서 초안"`)·`recipient`(선택)·
  `requestedOutcome`(선택, 기본값 `"초안 작성"`) 입력창을 추가.
- 상태 관리에 `extraInputs: Record<TabId, Record<string, string>>`를 추가하고 `buildBody`
  시그니처를 `(text, extra) => body`로 확장. `App.css`에 `.extra-fields`/`.extra-field` 스타일 추가.
- 기존 동작과의 호환성: 필드를 비워두면 이전과 동일한 기본값이 전송되므로, 아무것도 입력하지 않고
  제출해도 기존 UX가 깨지지 않음(회귀 없음).
- **라이브 브라우저 검증**: "당사자 지위"에 `을`, "검토 관심사항"에 `일방적 해지 조항` 입력 후
  실행 → 리포트의 "검토 범위"에 `당사자 지위: 을`, `검토 관심사항: 일방적 해지 조항`으로 정확히
  반영됨(이전엔 항상 `검토 요청자`/`일반 위험 검토`로 고정). "문서 유형"에 `내용증명`, "요청 결과"에
  `대금 지급` 입력 → "초안 범위"에 그대로 반영됨(이전엔 항상 `법률 문서 초안`/`초안 작성`으로 고정).
- **검증**: `npm run build`(백엔드/프론트엔드 둘 다), 프론트엔드 `vite build` 통과. 전체 vitest
  61개 통과. TC-2/TC-4/TC-5/TC-6 라이브 재검증으로 회귀 없음 확인.

### 이슈 #4 (Info, 낮음) — 응답 지연 시 시각적 피드백이 약함

- **재현 절차**: `LEGAL_PROVIDER=korean-law`에서 요청 실행 (약 2초 소요).
- **실제 결과**: 버튼 텍스트가 "실행 중"으로 바뀌고 비활성화되는 최소한의 피드백은 있으나, 스피너
  등 더 명확한 로딩 표시는 없음. 네트워크 상태가 느린 사용자에게는 응답이 없다고 오인될 여지가 약간 있음.
- **관련 케이스**: TC-7
- **심각도**: 낮음 — 현재 응답 시간(2~3초) 수준에서는 실사용에 큰 문제 없음.
